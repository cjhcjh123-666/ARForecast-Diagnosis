# ICLR 2027 分析文「Recognize, Don't Generate」—— 最终完整总结

> 生成时间：2026-08-24
> 论文命题：语言预训练对时序预测的真正价值是**可复用的时序结构先验**（擅长"识别"动力学），而非数值生成能力（"生成"脆弱）。
> 定位：ICLR 2027 分析类论文（不要求方法创新）。摘要截稿 2026-09-18，全文 2026-09-25（AOE）。

---

## 目录
1. 一句话命题与核心贡献
2. 决策轨迹与时间线
3. 可控实验协议（全部实验共用的基础设施）
4. 实验全景表
5. 各实验详细结果（含完整数据表）
   - E1 受控消融：语言预训练确实有用
   - E2 冻结识别：预训练 >> 随机
   - R2b 逐层 probe：结构分布全栈 vs 浅层衰减
   - R2c 微调前后表示：不降反升但重排
   - E3 识别 vs 生成（同窗口）
   - E3b 语法约束解码
   - E3c 生成鲁棒性（prompt/解码全变体）
   - E4 零样本路由 + 非 LLM 对照（PatchTST/MLP/Chronos）
   - R5 家族 × 规模表（GPT-2/DistilGPT2/Qwen3 0.6-8B）
   - R6 真实数据路由（15 数据集 × 4 模型）+ 多 horizon
   - E6 动态路由
   - 难度扫描（context/noise/horizon）
   - 统计严谨性（bootstrap CI + 5 seeds）
6. 图表清单（Times New Roman 规范）
7. 脚本清单与产物位置
8. 论文状态（main.tex / 模板 / PDF 编译）
9. 基础设施与踩坑记录
10. 已做 vs 待办
11. 下一步建议
12. Git 提交记录

---

## 1. 一句话命题与核心贡献

**"Recognize, Don't Generate"（识别，不要生成）**：
把 LLM 做时序预测拆成两种能力——**识别**时序结构（trend/periodic/local/mixture/regime）与**生成**未来数值。
受控实验表明：语言预训练大幅提升**识别**（含 OOD 迁移），但对冻结模型的**数值生成**没有帮助（5-15× 误差、parse 不稳定），
且该不对称性在模型家族（GPT-2/Qwen3）× 规模（82M-8B）× 难度（context/noise/horizon）× prompt/解码（greedy/采样/few-shot/指令）下全部成立。

**对审稿争议的答复**：
- Tan 2024（说没用）vs 2025-2026 新工作（说有用）的矛盾 → 两边都部分正确，因为把"识别"和"生成"混为一谈。
- "要不要 LLM？" → 同数据训练的 PatchTST/MLP 域内 99% 但 OOD 路由仅 13-34%；**专业时序基础模型 Chronos 域内 100% 但 OOD 路由仅 9-36%**；
  只有冻结预训练 LLM（8B）OOD 路由达到 67.8%。**迁移来自语言预训练表示，与模型容量/时序预训练规模无关。**

---

## 2. 决策轨迹与时间线

| 阶段 | 决策 | 说明 |
|---|---|---|
| 最早 | ICASSP 2027（4 页 spconf） | 老板最初打算投 ICASSP |
| ICASSP 补齐 | E1b/E3b/E4b/E5b | 应审稿意见补小模型 2×2、语法约束、learned router、多域路由 |
| 转折 | **改投 ICLR 2027（分析类）** | 老板定调：投 ICLR，不要求方法创新，做完整分析 |
| ICLR 扩展 | P0/P1/P2 全部铺开 | 规模、跨家族、表示机制、数据广度、统计严谨性、理论化 |
| 2026-08-24 | P0.2 生成鲁棒性、P1.4 多 horizon、P1.5 可视化完成 | 全部实验闭环 |
| 2026-08-24 | 装 LaTeX（tectonic）+ 编译 ICLR 模板 | 中途因 PDF 文本层异常暂停，详见 §8/§9 |

---

## 3. 可控实验协议（全部实验共用）

- **动力学**：trend（线性+噪声）、periodic（周期 12/24 正弦）、local（稳定 AR(1), φ∈[0.7,0.95]）、mixture（趋势+正弦）、regime（前 60% 趋势后转正弦）。
  前 3 类为"干净"，后 2 类保留为 **OOD 测试**。
- **窗口**：context=64，horizon=16（除多 horizon 实验用 24/48）。按 context 标准化。
- **文本协议**：`History: +0.12 -0.04 ... Forecast:`，数值 clamp [-9.99,9.99]、两位小数。
- **Probe**：冻结模型 last-token hidden → 线性 softmax probe（numpy 梯度下降 2000 步）。
- **特征 floor**：9 个手算统计量（斜率、线性 R²、波动率、lag-1 自相关、主周期/强度、谱熵、谱峰集中度、极差比）。
- **专家**：trend=稳健线性外推、periodic=自相关周期检测+季节 naive、local=阻尼 AR(5)。oracle=各窗口实际误差最小的专家。
- **模型**：Qwen3-0.6B/1.7B（vanilla）、Wave-MoE Qwen3-8B（语言主干）、GPT-2 124M、DistilGPT2 82M。随机对照=同架构重初始化。
- **Router**：probe 训练于 3 干净类 → 零样本路由到未见 mixture/regime（E4）；或路由到真实数据窗口（R6）。

---

## 4. 实验全景表

| # | 实验 | 内容 | 一句话结论 |
|---|---|---|---|
| E1 | 受控预报消融 | 只换初始化 × LoRA/全量 × 3 数据 × 3 seeds | 预训练比随机 MSE 降 2.2-25×，非 LoRA 伪影 |
| E2 | 冻结结构识别 | 5 类动力学线性 probe | 预训练 96-98% vs 随机 79-91% |
| R2b | 逐层 probe | 29 层每层 probe | 预训练全栈平台 98%；随机浅层峰值 90%→深层 79% |
| R2c | 微调前后 | 8B+ETTm1 LoRA | 识别 0.967→0.983 不降反升；CKA 0.45、probe 方向 cos 0.08 |
| E3 | 识别 vs 生成 | 同窗口配对 | 识别 ~97% vs 生成 5-15×、parse 27-98% |
| E3b | 语法约束 | 强制合法数值 token | parse=100%，误差仍 6.2×（非格式问题） |
| E3c | 生成鲁棒性 | greedy/采样/few-shot/指令 | 所有变体下 3.5-6.8×，采样/few-shot 无效 |
| E4 | 零样本路由 | 3 干净→未见 mixture/regime | LLM 8B 67.8% vs 特征 12.8% vs 随机 28.9% |
| E4b | 非 LLM 对照 | PatchTST/MLP/Chronos | 域内 99-100% 但 OOD 全崩（13-36%） |
| R5 | 家族×规模 | GPT-2/DistilGPT2/Qwen3 0.6-8B | 识别全稳、生成 5-15×、路由 8B 最强 |
| R6 | 真实数据路由 | 15 数据集 × 4 模型 | LLM 胜 8/15，失败集一致（电/流量/M4） |
| R6b | 多 horizon | 8B，H24/H48，5 数据集 | LLM 4/5 胜，与 H16 一致 |
| E6 | 动态路由 | patch 级路由 | regime -33%、local -20%、平稳无收益 |
| 难度扫描 | context/noise/horizon | 识别稳、生成比随 horizon 涨 | 支持 ρ^H 形式化 |
| 统计 | bootstrap CI + 5 seeds | 2000 次重采样 | 8B 路由 CI [0.62,0.78] 与对照不重叠 |

---

## 5. 各实验详细结果

### E1 受控预报消融（LoRA，3 seeds: 7/17/27）

| 数据集 | 预训练 MSE | 随机 MSE | 相对降幅 |
|---|---|---|---|
| synthetic_sine | 0.0094 ± 0.0006 | 0.0212 ± 0.0011 | 2.2× |
| ETTm1 | 0.0099 ± 0.0017 | 0.2453 ± 0.1366 | 25× |
| ETTh1 | 0.0487 ± 0.0097 | 0.2896 ± 0.2059 | 6× |

小模型 2×2（0.6B/1.7B × LoRA/full-FT，`results/icassp/small_e1/`）：
随机 + 全量微调仍差 10-13× → **排除"随机 8B + rank-8 LoRA 学不动"的公平性质疑**。
预训练 parse 率全 1.0、方差小；随机方差大、偶有 parse 失败。

### E2 冻结结构识别（5 类，3 seeds）

| 模型 | 预训练 | 随机 | 特征 floor |
|---|---|---|---|
| DistilGPT2 | 0.973±0.009 | 0.744±0.022 | 0.990 |
| GPT-2 | 0.980±0.008 | 0.699±0.041 | 0.990 |
| Qwen3-0.6B | 0.984±0.006 | 0.789±0.010 | 0.993 |
| Qwen3-1.7B | 0.979±0.011 | 0.837±0.012 | 0.990 |
| Qwen3-8B | 0.963±0.005 | 0.906±0.009 | 0.990 |

→ 识别全稳；预训练优势 GPT-2 家族最大（~28% gap）；干净合成数据大家都高（特征 floor ~99%），
**预训练优势体现在 OOD 迁移（E4）而非域内分类**。

### R2b 逐层 probe（Qwen3-0.6B，29 层含 embedding）

- 预训练：层 0（embedding）0.20（chance）→ 层 1 起 ~0.93 → 上层平台 **0.98**
- 随机：层 2-3 峰值 ~0.90 → 深层**衰减到 0.79**
- 结论：语言预训练让时序结构信息**分布全栈**；随机网络只有浅层有、随深度丢失。
  数据：`results/iclr/layerwise/qwen3_0.6b{,_random}/summary.json`

### R2c 微调前后表示（8B + ETTm1 LoRA adapter, seed7）

| 指标 | 冻结 | 微调后 |
|---|---|---|
| 识别准确率 | 0.967 | **0.983**（local 0.95→0.98, mixture 0.92→0.98） |
| 表示 CKA | — | 0.453 |
| probe 方向 cos | — | 0.075 |
| mean abs delta | — | 0.692 |

→ 预测微调**不侵蚀识别表示，反而轻微锐化**，但表示几何被大幅重写。
数据：`results/iclr/prepost_ft/ettm1_seed7/summary.json`

### E3 识别 vs 生成（同窗口，3 seeds）

| 模型 | 识别 | 生成 parse | 生成 MSE | oracle MSE | 比值 |
|---|---|---|---|---|---|
| Qwen3-0.6B | 0.98 | 0.980 | 3.31 | 0.34 | 9.9× |
| Qwen3-1.7B | 1.00 | 0.592 | 2.73 | 0.34 | 8.2× |
| Qwen3-8B | 0.97 | 0.267 | 1.85 | 0.33 | 5.5× |
| GPT-2 | 0.98 | 1.000 | — | — | 12.4× |
| DistilGPT2 | 0.97 | 1.000 | — | — | 15.4× |

→ **识别鲁棒、生成脆弱**。GPT-2 parse 100% 但误差比最大（12-15×）→ 生成失败不是格式问题。

### E3b 语法约束解码（Qwen3-0.6B/8B）

| 解码 | parse | MSE/oracle |
|---|---|---|
| 自由贪心 | 0.267 | 5.5× |
| 语法约束 | 1.000 | 6.2× |

→ 强制合法数值 token 后 parse=100%，误差比仍 6.2× → **排除格式/序列化失败**。

### E3c 生成鲁棒性（P0.2，同窗口全变体）

| 变体 | Qwen3-0.6B parse/ratio | Qwen3-8B parse/ratio |
|---|---|---|
| greedy | 1.000 / 6.0× | 0.425 / 4.9× |
| temp 0.7 | 0.875 / 4.2× | 0.375 / 5.7× |
| temp 1.0 | 0.650 / 3.5× | 0.425 / 4.6× |
| 显式指令 | 0.975 / 5.7× | 0.175 / 6.2× |
| few-shot 1 | 0.925 / 4.5× | 0.900 / 4.1× |
| few-shot 2 | 1.000 / 6.8× | 0.875 / 5.8× |

→ **所有 prompt/解码变体下误差比保持 3.5-6.8×**；采样不救、few-shot 提 parse 但误差比不变、指令伤 8B。
"don't generate" 与格式/解码/prompt 无关，是**数值续写能力缺陷**。
数据：`results/iclr/gen_robustness/qwen3_{0.6b,8b}/summary.json`

### E4 零样本路由（3 干净 → 未见 mixture/regime，8B，3 seeds）

| 路由 | OOD 命中率 | MSE | Soft MSE |
|---|---|---|---|
| Oracle（上界） | — | 0.452 | — |
| Best single（trend） | — | 0.492 | — |
| Uniform | — | 1.346 | — |
| Hand features | 0.128 | 1.278 | 1.235 |
| Random Qwen probe | 0.289 | 1.079 | 1.056 |
| PatchTST（域内 ~99%） | 0.133-0.217 | 1.36-1.76 | — |
| MLP（域内 ~99%） | 0.275-0.342 | 0.83-1.17 | — |
| **冻结预训练 Qwen-8B** | **0.678** | **0.739** | **0.723** |

→ **同数据训练的监督分类器 OOD 全崩；迁移来自语言预训练表示。**

### E4b 预训练时序编码器对照（Chronos，P0-4a 强扩展）

| 模型 | 域内识别 | 域内路由 | OOD 路由 | OOD MSE |
|---|---|---|---|---|
| Chronos-T5-small | 1.000 | 0.611 | 0.092 | 1.620 |
| Chronos-T5-base | 1.000 | 0.611 | 0.292 | 0.912 |
| Chronos-T5-large | 0.997 | 0.611 | 0.200 | 1.043 |
| Chronos-Bolt-small | 1.000 | 0.611 | 0.300 | 0.894 |
| Chronos-Bolt-base | 1.000 | 0.611 | 0.308 | 0.866 |
| Chronos-Bolt-tiny | 1.000 | 0.611 | 0.358 | 0.814 |
| **冻结 Qwen-8B** | 0.963 | — | **0.678** | **0.739** |

→ 专业时序基础模型（value-token 的 T5 与 patch 的 Bolt，8M-300M）**域内 100% 但 OOD 路由 9-36%**，
从未接近 LLM。**路由优势与模型容量/时序预训练规模无关，来自语言预训练**。
代码：`third_party/chronos-forecasting/`（官方包）+ `scripts/iclr_chronos_probe.py` + `scripts/iclr_chronos_bolt_probe.py`。
数据：`results/iclr/chronos/*`

### R5 家族 × 规模表（3-5 seeds）

| 模型 | 家族 | 识别(预) | 识别(随机) | 生成 parse | 生成/oracle | 路由(预) |
|---|---|---|---|---|---|---|
| DistilGPT2 | GPT | 0.973 | 0.744 | 1.000 | 15.4× | 0.181 |
| GPT-2 | GPT | 0.980 | 0.699 | 1.000 | 12.4× | 0.194 |
| Qwen3-0.6B | Qwen | 0.984 | 0.789 | 0.980 | 9.9× | 0.337 |
| Qwen3-1.7B | Qwen | 0.979 | 0.837 | 0.592 | 8.2× | 0.308 |
| Qwen3-8B | Qwen | 0.963 | 0.906 | 0.267 | 5.5× | 0.678 |

（0.6B 为 5 seeds；其余 3 seeds；8B 数据来自 ICASSP 目录）
→ 识别全稳；**生成 parse 随规模下降**（98%→57%→27%）而误差始终 5-15×；
**路由优势在 8B 最大**（0.678 vs 随机 0.289 vs 特征 0.128）。

### R6 真实数据零样本路由（15 数据集，seed7）

**Qwen3-8B**（`results/iclr/multi_dataset_router/qwen3_8b/summary.json`）：

| 数据集 | oracle | best single | 特征路由 MSE | LLM 路由 MSE |
|---|---|---|---|---|
| ETTm1 | 0.011 | periodic/0.015 | 0.521 | **0.022** |
| ETTh1 | 0.028 | periodic/0.045 | 0.581 | **0.047** |
| ETTh2 | 0.114 | periodic/0.211 | 0.610 | **0.218** |
| ETTm2 | 0.049 | periodic/0.064 | 0.327 | 0.151 |
| Exchange | 0.017 | periodic/0.027 | 1.591 | **0.027** |
| Weather | 0.021 | periodic/0.025 | 0.482 | **0.082** |
| Illness | 0.554 | trend/1.067 | 4.620 | **1.455** |
| Electricity | 0.407 | local/0.855 | **0.740** | 0.927 |
| Traffic | 0.680 | periodic/0.883 | **0.771** | 0.916 |
| M4-Daily | 0.988 | local/1.213 | **1.244** | 1.826 |
| M4-Hourly | 211.9 | periodic/214.0 | 214.8 | **214.0** |
| M4-Weekly | 1.320 | local/1.419 | **1.419** | 2.524 |
| M4-Monthly | 1.078 | local/1.218 | **1.218** | 2.140 |
| M4-Quarterly | 1.083 | local/1.233 | **1.233** | 1.996 |
| M4-Yearly | 1.041 | local/1.164 | **1.164** | 1.938 |

**四模型胜场统计**（LLM vs 特征路由）：
- Qwen3-0.6B：8 胜 7 负；Qwen3-1.7B：8 胜 7 负；Qwen3-8B：~6 胜；GPT-2：8 胜 7 负
- **失败集高度一致**：Electricity / Traffic / M4 多数频率（同质或弱结构数据）
- 结论：LLM 路由在**多样性动力学**数据集上大幅胜出（ETT×4/Exchange 59×/Weather/Illness），
  在**同质周期**数据上追平"恒选 periodic"（无害），在**高维复杂通道**（电/流量）与 **M4 宏观短序列**上失败（诚实边界）。

### R6b 多 horizon（P1.4，8B，H24/H48，复用同 hidden）

| 数据集 | H | oracle | best | 特征 | LLM |
|---|---|---|---|---|---|
| ETTm1 | 24 | 0.015 | 0.019 | 0.596 | **0.028** |
| ETTm1 | 48 | 0.027 | 0.034 | 0.757 | **0.051** |
| ETTh1 | 24 | 0.035 | 0.056 | 0.672 | **0.061** |
| ETTh1 | 48 | 0.050 | 0.071 | 0.842 | **0.083** |
| Weather | 24 | 0.035 | 0.044 | 0.521 | **0.123** |
| Weather | 48 | 0.112 | 0.148 | 0.579 | **0.317** |
| Exchange | 24 | 0.026 | 0.036 | 1.944 | **0.036** |
| Exchange | 48 | 0.046 | 0.060 | 2.497 | **0.060** |
| Electricity | 24 | 0.465 | 0.792 | **0.668** | 0.818 |
| Electricity | 48 | 0.576 | 0.910 | **0.792** | 0.943 |

→ **长 horizon 下 LLM 路由 4/5 胜，Exchange H48 = best-single（0.060）**，仅 Electricity 保持特征优。
数据：`results/iclr/multi_horizon/qwen3_8b/summary.json`

### E6 动态路由（patch 级，LLM probe）

| 窗口类型 | 静态最优 | 动态路由 | Δ |
|---|---|---|---|
| regime | 0.82 | 0.55 | -33% |
| local | 1.00 | 0.80 | -20% |
| 平稳 | 1.00 | 1.00 | 0% |

→ **动力学异质时路由获益、同质时中性**（诚实的边界条件）。

### 难度扫描（P1-1a，Qwen3-0.6B，`results/iclr/asymmetry/qwen3_0.6b/`）

| 轴 | 设置 | 识别 | 生成 parse | 生成/oracle |
|---|---|---|---|---|
| context | 32 | 0.942 | 0.975 | 5.8× |
| context | 64 | 0.983 | 1.000 | 7.9× |
| context | 128 | 0.983 | 1.000 | 8.6× |
| noise | 0.05 | 0.983 | 1.000 | 7.9× |
| noise | 0.10 | 0.958 | 1.000 | 7.8× |
| noise | 0.20 | 0.908 | 1.000 | 7.1× |
| horizon | 8 | 0.967 | 1.000 | 6.5× |
| horizon | 16 | 0.983 | 1.000 | 7.9× |
| horizon | 32 | 0.992 | 0.875 | 8.4× |

→ 识别对噪声稳健（98%→91%）、随 horizon 上升；**生成比随 horizon 从 6.5× 涨到 8.4×**（支持 ρ^H 形式化）。

### 统计严谨性（P0-5a，bootstrap CI 2000 次）

- 8B 路由：预训练 0.678 [0.617, 0.783] vs 随机 0.289 [0.267, 0.308] vs 特征 0.128 [0.108, 0.142] —— **CI 完全不重叠**
- 生成/oracle 比：5.5× [4.0, 6.5]
- 0.6B（5 seeds）：路由 0.337 [0.282, 0.395] vs 随机 0.217 [0.200, 0.235] vs 特征 0.162 [0.132, 0.192]
- 数据：`results/iclr/bootstrap_ci.json`（脚本 `scripts/iclr_bootstrap_ci.py`）

---

## 6. 图表清单（Times New Roman 规范）

`paper/iclr2027/figs/`，全部用 `scripts/iclr_plot_style.py` 规范：
**字体 Times New Roman（Linux 用 Liberation Serif 度量兼容回退，本地有真 TNR 自动用）**；字号正文 9pt / 标题 10pt / 刻度 8pt / 图例 8pt；DPI 300。

| 图 | 内容 |
|---|---|
| fig1_recognize_vs_generate.pdf | 识别 vs 生成 parse 与误差比（家族×规模） |
| fig2_zero_shot_routing.pdf | E4 各方法 OOD 命中率柱状（含 Chronos） |
| fig3_layerwise.pdf | 预训练 vs 随机逐层 probe 曲线 |
| fig4_real_data_routing.pdf | 8B 15 数据集 log-MSE 分组柱状 |
| fig5_asymmetry_sweeps.pdf | context/noise/horizon 三轴扫描 |
| fig6_representation_tsne.pdf | 预训练 vs 随机 hidden 的 PCA/t-SNE（5 类） |

---

## 7. 脚本清单与产物位置

| 脚本 | 用途 |
|---|---|
| `scripts/iclr_multi_dataset_router.py` | 15 数据集零样本路由（0.6B/1.7B/8B/GPT-2） |
| `scripts/iclr_multi_dataset_from_cache.py` | 用缓存 hidden 计算路由（免模型加载） |
| `scripts/iclr_multi_horizon.py` | 多 horizon 路由（hidden 跨 horizon 复用） |
| `scripts/iclr_learned_router.py` | PatchTST/MLP 学习式路由 baseline |
| `scripts/iclr_chronos_probe.py` | Chronos-T5（value-token）probe |
| `scripts/iclr_chronos_bolt_probe.py` | Chronos-Bolt（patch）probe |
| `scripts/iclr_layerwise_probe.py` | 逐层 probe |
| `scripts/iclr_prepost_ft.py` | 微调前后表示对比 |
| `scripts/iclr_asymmetry_sweep.py` | context/noise/horizon 扫描 |
| `scripts/iclr_gen_robustness.py` | 生成鲁棒性（采样/few-shot/指令） |
| `scripts/iclr_router_from_cache.py` | 从缓存算合成路由 |
| `scripts/iclr_bootstrap_ci.py` | bootstrap CI |
| `scripts/iclr_aggregate.py` | 汇总所有结果 → `results/iclr/AGGREGATE.md` |
| `scripts/iclr_make_figures.py` | 生成全部图 |
| `scripts/iclr_plot_style.py` | 共享字体/字号样式 |

结果根目录：`results/iclr/`（probe/layerwise/prepost/asymmetry/gen_robustness/chronos/learned_router/multi_dataset_router/multi_horizon/router_from_cache/bootstrap_ci.json/AGGREGATE.md）。
ICASSP 旧结果：`results/icassp/`（8B probe/E1/small_e1 等）。

---

## 8. 论文状态

- **论文**：`paper/iclr2027/main.tex`（568+ 行，全部真实数据填充、占位符清零）+ `refs.bib`。
- **模板**：`docs/iclr2027.zip` → `paper/iclr2027_template/iclr2027/`（iclr2027_conference.sty/bst、natbib、fancyhdr、math_commands）。
  已把模板文件拷入 `paper/iclr2027/` 并把 main.tex 前言改为 `\usepackage{iclr2027_conference,times}` + `\input{math_commands.tex}`，bibstyle 改 `iclr2027_conference`。
- **LaTeX 环境**：装好 **tectonic 0.17.0**（`/public/duyinglong/miniconda3/envs/latex/bin/tectonic`，从 GitHub 走代理下载的静态二进制）。
- **编译**：`tectonic -X compile main.tex` 已成功产出 10 页 PDF（`paper/iclr2027/main.pdf`）。
  **⚠️ 已知问题**：PDF 文本层出现 000,001,...,500 的连续数字幽灵序列，且 fitz 检测不到内嵌图片。
  视觉内容（标题/摘要/正文，LMRoman 字体）经 fitz 逐块检查是**正确的**；怀疑是 tectonic(XeTeX) 下 `times`/字体 ToUnicode CMap 或 figure 路径问题导致提取异常。
  **结论：编译链可用，但 PDF 文本层/图片内嵌需要进一步排查**（用户已叫停此项，暂搁置）。
- **在线查看 PDF**：未完成（原本计划用 Sites 托管，因 PDF 问题暂停）。

---

## 9. 基础设施与踩坑记录（重要）

1. **GPU**：8×A800（80GB），竞争激烈。GPU 0-3 常被其他用户占满（~72GB），8B 任务易 OOM。
   可用 `nvidia-smi --query-gpu=index,memory.free` 动态选卡；0.6B/1.7B 小模型弹性大。
2. **网络（重大发现）**：
   - 这台机器**网络是好的**！之前测不通是因为：沙箱内回环被隐藏（连不上 127.0.0.1:7890）+ 沙箱外我又错误地 `env -u https_proxy` 把代理去掉直连。
   - **正确姿势**：`export https_proxy=http://127.0.0.1:7890 http_proxy=...`，且命令用 **escalated（沙箱外）** 执行。
   - 代理是 **mihomo**（PID 3091367），监听 127.0.0.1:7890；配置在 `/home/zhangyafei/clashctl/`（另有 `/home/yangfan/clash/`）。
   - 通过代理：HF / hf-mirror / pypi / conda / GitHub API 全部可达。
3. **模型来源**：
   - Qwen3-0.6B/1.7B：`/9950backfile/chenjiahui/hf_cache/`（本地完整）。
   - Qwen3-8B：`/public/chenjiahui/Wave-MoE-Skill-Agent/hf_models/Qwen3-8B`（Wave-MoE Qwen3TS 自定义 checkpoint，只用语言主干）。
   - **GPT-2/DistilGPT2**：`/public/qbwang/public/gpt2{,_distil}` 有完整权重（`.bin` 是 pickle，torch 2.4.1 拒绝加载 → 已转成 safetensors 放在 `models/gpt2{,_distilgpt2}/`）。
   - **Chronos T5/Bolt**：`/public/chenjiahui/SAFER-TS/code/model_cache/amazon__chronos-*`（官方权重，含 chronos_config）。
4. **Qwen3 加载修复**：`ALL_PARALLEL_STYLES` monkeypatch（`models/qwen_text_ar.py` 与 `scripts/run_frozen_probe.py` 已加）。
5. **nohup 会被杀**：沙箱外 nohup 后台进程随 exec 退出被清理；**tmux `-S /tmp/iclr-tmux` 持久会话**才可靠。
6. **进程自匹配坑**：`pkill -f "模式"` 会匹配到自己的 shell → 用 `ps -ef | grep "[p]attern"` 避免。
7. **pytorch CPU 线程 thrashing**：负载高（load 349）时 numpy/torch 多线程会互相拖死 → 设 `OMP_NUM_THREADS=4 MKL_NUM_THREADS=4` 可大幅提速（8B from-cache 计算从 1 小时降到几分钟）。
8. **8B 反复挂死**：负载极高时 1.7B/8B generation 会卡死（CUDA 饿死）；降低并发、单任务串行 + timeout 包装可缓解。
9. **conda latex env 损坏**：缺整个 tex/ 宏包树 + mktexlsr.pl 被错链成 shell 脚本，无法修复 → 改用 tectonic。
10. **Times 字体**：Linux 无真 Times New Roman，用 Liberation Serif（度量兼容）回退；matplotlib 设 `font.serif=[Times New Roman, Liberation Serif, ...]`，本地有真字体时自动生效。
11. **PyMuPDF**：pip 走代理 SSL 崩 → 用 curl 下载 wheel + `pip install --no-index` 安装成功（`third_party/wheels/`）。

---

## 10. 已做 vs 待办

### ✅ 已完成
- 全部核心实验（E1/E2/E3/E3b/E3c/E4/E4b/R5/R6/R6b/E6/难度扫描/统计）
- 跨家族（GPT-2/DistilGPT2）与专业时序编码器（Chronos T5/Bolt×6）对照
- 5 seeds + bootstrap CI；Times New Roman 图表 + PCA/t-SNE
- ICLR 论文 main.tex（全部数据填充）+ 6 图 + refs.bib
- 文档：`docs/ICASSP2027_完整总结.md`、`docs/ICLR2027_完整总结.md`、本文件
- LaTeX：tectonic 装好、ICLR 模板接入、10 页 PDF 已产出
- 网络通道打通（代理用法）

### ⏳ 待办/未完成
1. **PDF 文本层异常排查**（幽灵数字 000-500、图片未内嵌）——用户叫停，暂搁置。
2. **在线打开编译后 PDF**（计划用 Sites 托管，未做）。
3. **P0.1 理论化**：把 "Formalization sketch" 升级为正式小节（识别迁移性指标 + 异质性 H(X) vs 路由收益的 15 数据集散点验证）。
4. **P0.3 与 Tan 2024 / manifold 2026 / random-init 2025 的调和章节**（写作）。
5. 论文 P2 打磨：Related Work 扩充、主图精修、审稿预演。
6. Chronos-Bolt 的 in-domain 路由 0.611 恒定问题可再查（疑似恒选 baseline）。

---

## 11. 下一步建议（按优先级）

1. **修 PDF**：换引擎（tectonic 用 `-X compile --engine` 或改用 pdflatex 模式 / 或 conda 装 texlive 修复），或先不管文本层、只保证视觉正确。
2. **在线托管**：用 Sites 插件把 PDF + 论文源码托管，给用户一个可打开的 URL。
3. **P0.1 异质性散点**：数据现成（15 数据集 per-expert MSE），做 H(X) vs LLM 路由收益散点 + 理论小节。
4. **P0.3 调和章节**：写清三方结论差异的统一解释。
5. 全文精修 + 本地编译 + 对照 ICLR 页数限制（9 页正文 + 附录）。

---

## 12. Git 提交记录（最新 15 条）

```
11895d2 feat: P0.2 gen robustness (all prompt/decoding variants 3.5-6.8x), P1.4 multi-horizon routing (24/48, LLM wins 4/5), P1.5 Times New Roman figures + PCA/t-SNE (fig6)
29aaa5a feat: Chronos-Bolt (patch T5) probe - recog 100% but OOD routing 30-36% across small/base/tiny, paper E4 + docs updated
5f88187 fix: clean chronos vendored package structure (src/chronos + LICENSE)
691f5bb fix: track chronos pkg source (not embedded repo)
66ac86d feat: Chronos-T5 pretrained time-series encoder baseline (in-domain recog 100% but OOD routing 9-29% vs LLM 67.8%)
96cff74 feat: complete ICLR experiments - 1.7B seed7 gen fix + 1.7B 15-dataset router (8/15 wins), family x scale table final
3657759 feat: cross-family ICLR - GPT-2/DistilGPT2 probes (recog 97-98% vs rand 70-74%, gen 12-15x oracle)
73beda3 feat: ICLR complete - 5-seed stats, asymmetry sweeps, layerwise+prepost analysis, 15-dataset router, learned-router baselines, figures, paper, docs
fceac87 feat: ICLR expansion - model scale, 15-dataset router, learned-router baselines, layerwise probe, prepost-FT, asymmetry sweeps, bootstrap CI, paper draft
ed57248 feat: E3 grammar-constrained generation (parse 100%, MSE 6.2x oracle); paper updates for E1b/E4b/E5b
b7e86aa feat: reviewer-requested supplements - E1 small-model 2x2, E4 learned-router baseline, E5 multi-domain routing, Qwen3 load fix
0acbb87 docs: add comprehensive ICASSP 2027 design+experiments summary
9a82b0a docs: switch paper to ICASSP spconf template
517081a feat: patch-level dynamic routing experiment + E6 paper section
a5c741a feat: 3-seed probe aggregation, ETTh1 real-data routing reproduction
```

---
*本文件为 ICLR 2027 工作唯一权威汇总。数据细节在各实验 JSON 与 `results/iclr/AGGREGATE.md`。*

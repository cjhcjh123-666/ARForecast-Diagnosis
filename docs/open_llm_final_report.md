# Open-LLM Cross-Family Attribution Suite — Final Report

_Generated by `scripts/openllm_final_report.py` from result CSVs (2026-09-14). Companion documents: `open_llm_all_results.md` (full tables), `open_llm_summary_zh.md` (中文摘要), `open_llm_failures_and_blockers.md`, `open_llm_reproducibility.md`._

## Model matrix (requested format)

| Model | Completed | Family-label Δ | Oracle-label Δ | 5-expert Δ | Real-world P/R (wins vs random) | Native MSE / oracle |
|---|---|---:|---:|---:|---:|---:|
| **Qwen3-8B** | yes | +0.311 | -0.194 | — | — | — / — |
| **Llama-3.1-8B** | yes | +0.228 | -0.172 | — | 14/15 | 2.992 / 8.91× |
| **Llama-3.2-3B** | yes | +0.147 | -0.122 | +0.063 | 15/15 | 4.7936 / 14.28× |
| **Gemma-2-9B** | yes | +0.200 | -0.056 | +0.103 | 13/15 | 2.9464 / 8.78× |
| **Gemma-2-2B** | yes | +0.150 | -0.133 | +0.140 | 12/15 | 3.218 / 9.59× |
| **Mistral-7B-v0.3** | yes | +0.106 | -0.131 | — | 13/15 | 1.9809 / 5.90× |
| **DeepSeek-LLM-7B** | yes | +0.075 | -0.214 | — | 13/15 | 1.3876 / 4.13× |
| **OLMo-2-7B** | yes | +0.094 | -0.122 | — | 13/15 | 1.8981 / 5.65× |
| **OLMo-2-13B** | yes | +0.083 | -0.172 | — | 13/15 | 1.4095 / 4.20× |
| **DeepSeek-V2-Lite (MoE)** | yes | +0.314 | -0.150 | — | 14/15 | 3.924 / 11.69× |

Δ = pretrained − random (balanced accuracy, mean over seeds 7/17/27); real-world column = number of the 15 datasets where the pretrained router has lower zero-shot routed MSE than its matched random control. The 5-expert column exists only for the models for which that sensitivity was run; `—` is *not measured*, not negative.

## Main findings

1. **Cross-family structure transfer (10/10).** With family-label supervision on the 270 clean primitive windows, pretrained beats matched random on balanced OOD compositional routing for every base LM tested (+7.5 to +31.4pp), and on the second OOD set (`bal3n`) too. Clean recognition and the shuffle control also favour pretrained for all models, i.e. the effect is not a Qwen artefact and is not explained by marginal value statistics (shuffling the 64 time positions destroys most of the advantage).
2. **The conversion to a decision is interface-dependent (10/10 flip).** Re-labelling the *same* clean windows with the realised-future best expert (`oracle-label`) turns the routing advantage negative for all ten models (−5.6 to −21.4pp). Family↔oracle agreement on clean windows is only ≈64%. Interpretation: pretraining organises a stable latent partition, not a realisation-level expert choice.
3. **The effect survives on real data (9/9 models).** Zero-shot routers (trained only on synthetic clean primitives) beat matched random controls on 12–15 of 15 datasets per model, with BH-FDR significance on most; median relative routed-MSE reductions of 12–28%. Pooled raw MSE across datasets is dominated by m4_hourly, so relative per-dataset changes are the reportable quantity. The honest caveat: against the hand-crafted temporal-feature router the same models only win 8–11/15 with median ≈0 to −4%, i.e. the real-data result establishes an advantage over random initialisation, not over an engineered feature baseline.
4. **Structure ≠ numerics.** The frozen numerical read-out and native generation tell a different story: the read-out advantage is model-dependent, and direct greedy generation is 4–15× worse than the oracle expert and *worse than the best fixed expert* for every local base LM. Matched random models never emit a parseable forecast (0.00 parse rate), so pretraining buys surface number formatting, not forecasting accuracy.

## Negative findings (retained, not hidden)

- Oracle-label supervision is uniformly worse for pretrained than random (see above) — the opposite sign of the family-label result.
- Time-series foundation models do **not** reproduce the LM-style transfer gain: Chronos-T5-base +0.11, Chronos-T5-small/Bolt ≈0, TimesFM −0.05, MOMENT −0.03, Moirai −0.06 (family-label, bal3).
- API-served chat models are worse at direct numerical forecasting than local base LMs: strict parse rate ≤33% (llama-3.1-70b), most 0–6%; parsed windows are 2.5–5.8× the best-fixed expert.
- Native generation for Gemma/DeepSeek needed environment fixes (see blockers doc); nothing was re-run to improve a number, and both crash logs are kept.

## Reviewer risks remaining

- The 5-expert / local-rich / dimension-matched (PCA, random projection) / pooling controls are incomplete for the new LMs; Qwen is missing the MLP-router and pooling columns.
- Only 3 seeds for the new LMs (Qwen has 10); the CIs quoted are seed-level nominal intervals, without multiplicity correction.
- All evidence is under one synthetic generator, one expert bank and one context/horizon setting (C=64, H=16); the real-world section is zero-shot routing, not a forecasting benchmark.

## Recommended claim

> Under our controlled protocol, weights obtained by language-model pretraining support a linearly readable, cross-family temporal structure that transfers to unseen temporal compositions and to real data, while the *magnitude* of the numerical benefit depends entirely on the downstream interface: it is positive for a stable structural partition, negative for realised-future expert labels, and absent for direct numerical generation.

This is Case B/C in the pre-registered decision tree: the effect is a recurring cross-family phenomenon (Case A direction) but its conversion into forecasting utility is interface-dependent (Case C), so the paper must not claim "language pretraining universally improves forecasting".

## Files produced

- `docs/open_llm_all_results.md` — full auto-generated report (Tables A/C/D/E + mechanism).
- `docs/open_llm_summary_zh.md`, `docs/open_llm_failures_and_blockers.md`, `docs/open_llm_reproducibility.md`.
- `figures/open_llm_suite/figA_forest.{png,pdf}`, `figB_robustness`, `figC_structure_vs_numerics`, `figD_realworld_heatmap`.
- `results/open_llm_suite/all_metrics_long.csv`, `all_metrics_wide.csv`, `paper_tables.tex`.
- `results/open_llm_suite/tables/robustness_matrix.csv`, `tableA_openllm_all.csv`, `tableD_summary.csv`, `figA_forest_data.csv`.
- `results/open_llm_suite/raw/tableD_realworld.csv`, `tableD_realworld_stats.csv`, `metrics_*.csv` (per run), `results/open_llm_suite/native/native_local.csv` (+ per-model NPZ).
- Scripts: `openllm_suite.py`, `openllm_native.py`, `openllm_realworld_features.py`, `openllm_realworld_analysis.py`, `openllm_make_report.py`, `openllm_figures.py`, `openllm_export.py`, `openllm_final_report.py`, `openllm_api_generate.py`.
